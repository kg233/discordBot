//a textModal class takes in a message, and an array of strings (option)
//option array should not have more than 6 elements (A through F)

class textModal {
  constructor(user, message) {
    this.text = text;
    this.options = options;

    this.selection = ['A: ', 'B: '];
  }

  getResponse() {}
}
